{
    "version" : "2017-02-28",
    "operation" : "Scan",
    #if(!$utils.isNull($ctx.args.nextToken))
    "nextToken" : "$ctx.args.nextToken",
    #end
    "limit" : $utils.defaultIfNullOrEmpty($ctx.args.limit, 10)
}
