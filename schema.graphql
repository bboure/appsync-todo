type Query {
  todos: [Todo!]!
  todo(id: ID!): Todo
}

type Todo {
  id: ID!
  title: String!
  createdAt: AWSDateTime
  items: [TodoItem!]!
}

type TodoItem {
  id: ID!
  title: String!
  done: Boolean!
}
