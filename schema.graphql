type Query {
  todos: [Todo!]!
  todo(id: ID!): Todo
}

type Mutation {
  createTodo(todo: CreateTodo): Todo!
}

type Todo {
  id: ID!
  title: String!
  createdAt: AWSDateTime
  items: [TodoItem!]
}

type TodoItem {
  id: ID!
  task: String!
  done: Boolean!
}

input CreateTodo {
  title: String!
}
